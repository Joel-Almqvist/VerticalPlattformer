package vertical_plattformer;

/**
 * Classes implementing the BoardListener interface may be added to listen to a Board
 * and will be notifies whenever said Board's board is changed.
 */
public interface BoardListener {
    void boardChange();
}
